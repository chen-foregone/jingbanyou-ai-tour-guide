<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.gdou.jingbanyou.manage.mapper.SysAdminMapper">

    <!-- 结果映射 -->
    <resultMap id="SysAdminResult" type="cn.edu.gdou.jingbanyou.manage.entity.SysAdmin">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="realName" column="real_name"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="lastLoginIp" column="last_login_ip"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 根据用户名查询 -->
    <select id="selectByUsername" resultMap="SysAdminResult">
        SELECT * FROM sys_admin 
        WHERE username = #{username} AND status = 1
    </select>

    <!-- 更新最后登录信息 -->
    <update id="updateLastLogin">
        UPDATE sys_admin 
        SET last_login_time = NOW(), 
            last_login_ip = #{ip}
        WHERE id = #{id}
    </update>

</mapper>
